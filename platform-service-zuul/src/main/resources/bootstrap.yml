eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
  instance:
    instance-id: ${spring.application.name}:${server.port}:${random.int}

spring:
  cloud:
    config:
      name: platform-service-zuul
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: platform-config-server
